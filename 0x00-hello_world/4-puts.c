#include <stdio.h>
/**
 *  Description: main -prints a string to stdout .
 *  Return: 0 if success.
 */
int main(void)
{
	puts("\"programming is like buliding a multilingual puzzle"
	    );
		return (0);
}
